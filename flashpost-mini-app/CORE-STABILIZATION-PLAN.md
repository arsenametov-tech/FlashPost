# 🚨 КРИТИЧЕСКИЙ ПЛАН СТАБИЛИЗАЦИИ ЯДРА

## СТАТУС: КРИТИЧЕСКАЯ АРХИТЕКТУРНАЯ НЕСТАБИЛЬНОСТЬ

**ПРИОРИТЕТ: СТАБИЛИЗАЦИЯ ЯДРА, НЕ ДОБАВЛЕНИЕ ФУНКЦИЙ**

## Задачи стабилизации (по приоритету)

### 1. ✅ Устранить дублирующие state
- **Проблема**: Множественные источники истины для данных слайдов
- **Решение**: Оставить единый `project.slides` как источник истины
- **Действие**: Удалить все дублирующие переменные состояния

### 2. ✅ Переписать render через DOM API
- **Проблема**: Смешанное использование HTML строк и createElement
- **Решение**: Полный переход на `createElement`, убрать все `innerHTML`
- **Действие**: Переписать все методы рендеринга на чистый DOM

### 3. ✅ Разделить режимы edit/preview/export
- **Проблема**: Смешанная логика режимов
- **Решение**: Четкое разделение поведения по режимам
- **Действие**: Отдельные методы для каждого режима

### 4. ✅ Отключить интерактивность в preview/export
- **Проблема**: Drag, input и панели работают во всех режимах
- **Решение**: Интерактивность только в edit режиме
- **Действие**: Условная привязка событий по режимам

### 5. ❌ Исправить ошибки из Problems
- **Проблема**: Синтаксические и логические ошибки
- **Решение**: Исправить все ошибки начиная с первых
- **Действие**: Проанализировать и исправить каждую ошибку

## Архитектурные принципы

### Единый источник истины
```javascript
// ✅ ПРАВИЛЬНО - единый state
this.project = {
    slides: [],           // единственный источник слайдов
    activeSlideId: null,  // текущий слайд
    activeTextBlockId: null, // текущий блок
    mode: 'start'         // режим приложения
};

// ❌ НЕПРАВИЛЬНО - дублирующие состояния
this.currentSlide = ...
this.slides = ...
this.activeSlide = ...
```

### Чистый DOM рендеринг
```javascript
// ✅ ПРАВИЛЬНО - createElement
const element = document.createElement('div');
element.className = 'slide';
element.appendChild(textBlock);

// ❌ НЕПРАВИЛЬНО - HTML строки
element.innerHTML = `<div class="slide">...</div>`;
```

### Режимо-специфичное поведение
```javascript
// ✅ ПРАВИЛЬНО - четкое разделение
if (mode === 'edit') {
    this.enableDragDrop();
    this.showToolbars();
} else {
    this.disableInteractivity();
}

// ❌ НЕПРАВИЛЬНО - смешанная логика
if (editable && !preview && mode !== 'export') { ... }
```

## План действий

1. **Анализ Problems** - получить список всех ошибок
2. **Очистка state** - удалить дублирующие переменные
3. **Рефакторинг рендеринга** - переписать на createElement
4. **Разделение режимов** - четкая логика по режимам
5. **Исправление ошибок** - по одной, начиная с критических
6. **Тестирование** - проверка стабильности после каждого исправления

## Запрещено до стабилизации

❌ Добавление новых функций
❌ Изменение API
❌ Новые компоненты
❌ Оптимизации производительности
❌ UI улучшения

## Разрешено только

✅ Исправление ошибок
✅ Упрощение архитектуры
✅ Удаление дублирующего кода
✅ Стабилизация существующего функционала

## Критерии завершения

- [ ] Нет ошибок в Problems
- [ ] Единый источник истины для данных
- [ ] Весь рендеринг через createElement
- [ ] Четкое разделение режимов
- [ ] Стабильная работа всех основных функций

**После выполнения этих пунктов — только потом возвращаться к новым функциям.**