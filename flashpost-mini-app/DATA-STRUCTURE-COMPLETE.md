# 📊 ПОЛНАЯ СТРУКТУРА ДАННЫХ ПРОЕКТА

## 🏗️ Архитектура: Project → Slides → TextBlocks → Styles

### 1. **Уровень Project (FlashPostApp)**

```javascript
class FlashPostApp {
    constructor() {
        // === ОСНОВНЫЕ ДАННЫЕ ===
        this.slides = [];              // ЕДИНЫЙ ИСТОЧНИК ДАННЫХ
        this.currentSlide = 0;         // Текущий слайд в карусели
        this.currentEditingSlide = 0;  // Текущий слайд в редакторе
        this.selectedTextBlockId = null; // ID выбранного текстового блока
        
        // === СОСТОЯНИЕ ПРИЛОЖЕНИЯ ===
        this.isGenerating = false;     // Флаг генерации
        this.applyToAll = false;       // Применить ко всем слайдам
        
        // === TELEGRAM WEBAPP ===
        this.tg = window.Telegram?.WebApp;
    }
}
```

### 2. **Уровень Slides (Массив слайдов)**

```javascript
this.slides = [
    {
        // === БАЗОВАЯ ИНФОРМАЦИЯ ===
        id: "slide_1704067200000_0",
        title: "Hook",
        text: "🔥 Секреты AI, которые изменят жизнь",
        
        // === AI КЛЮЧЕВЫЕ СЛОВА ===
        keywords: ["AI", "секреты", "жизнь"],        // Базовые ключевые слова
        autoKeywords: ["AI", "секреты", "жизнь"],    // AI ключевые слова для авто-подсветки
        
        // === ФОНОВОЕ ОФОРМЛЕНИЕ ===
        background: {
            type: "color",              // "color" | "image"
            color: "#833ab4",           // Цвет фона
            image: null,                // URL изображения или null
            brightness: 100,            // Яркость изображения (0-200%)
            position: { x: 0, y: 0 }    // Позиция изображения (-100% до +100%)
        },
        
        // === ТЕКСТОВЫЕ БЛОКИ ===
        textBlocks: [
            {
                // Базовая информация
                id: "block_1704067200000_abc123",
                text: "🔥 Секреты *AI*, которые изменят жизнь",
                
                // Позиционирование
                position: { x: 50, y: 50 }, // Позиция в процентах (0-100%)
                width: 80,                   // Ширина в процентах (20-100%)
                
                // Типографика
                font: "Inter",               // Шрифт
                size: 16,                    // Размер шрифта (10-32px)
                weight: 700,                 // Вес шрифта (300-900)
                color: "#ffffff",            // Цвет текста
                
                // Эффекты
                glow: false,                 // Эффект свечения
                isKeyword: false,            // Градиент всего блока
                
                // Подсветка ключевых слов
                highlightEnabled: true,      // Включить подсветку *слов*
                autoHighlight: true,         // Включить авто-подсветку от AI
                keywordColor: "#ff6b6b"      // Цвет подсветки ключевых слов
            }
            // ... другие текстовые блоки
        ],
        
        // === INSTAGRAM ЭЛЕМЕНТ ===
        instagram: {
            enabled: true,               // Показывать на первом/последнем слайде
            position: { x: 10, y: 10 }   // Позиция в процентах
        }
    }
    // ... другие слайды
];
```

## 📝 ПРИМЕРЫ РЕАЛЬНЫХ ОБЪЕКТОВ

### Пример 1: Полный объект слайда

```javascript
const exampleSlide = {
    // Базовая информация
    id: "slide_1704067200000_0",
    title: "Hook",
    text: "🔥 Секреты AI, которые изменят жизнь",
    
    // AI ключевые слова
    keywords: ["AI", "секреты", "жизнь", "технологии", "будущее"],
    autoKeywords: ["AI", "секреты", "жизнь", "технологии", "будущее"],
    
    // Фон - градиент
    background: {
        type: "color",
        color: "#833ab4",
        image: null,
        brightness: 100,
        position: { x: 0, y: 0 }
    },
    
    // Текстовые блоки
    textBlocks: [
        {
            id: "block_1704067200000_abc123",
            text: "🔥 Секреты *AI*, которые изменят жизнь",
            position: { x: 50, y: 30 },
            width: 85,
            font: "Montserrat",
            size: 20,
            weight: 800,
            color: "#ffffff",
            glow: true,
            isKeyword: false,
            highlightEnabled: true,
            autoHighlight: true,
            keywordColor: "#ffeb3b"
        },
        {
            id: "block_1704067200000_def456",
            text: "Узнайте, как *нейросети* изменят вашу работу",
            position: { x: 50, y: 70 },
            width: 75,
            font: "Inter",
            size: 14,
            weight: 600,
            color: "#f0f0f0",
            glow: false,
            isKeyword: false,
            highlightEnabled: true,
            autoHighlight: true,
            keywordColor: "#4ecdc4"
        }
    ],
    
    // Instagram элемент
    instagram: {
        enabled: true,
        position: { x: 10, y: 10 }
    }
};
```

### Пример 2: Слайд с фоновым изображением

```javascript
const slideWithBackground = {
    id: "slide_1704067200000_1",
    title: "Problem",
    text: "❌ Главная ошибка в AI",
    
    keywords: ["ошибка", "AI", "проблема"],
    autoKeywords: ["ошибка", "AI", "проблема", "решение"],
    
    // Фон - изображение
    background: {
        type: "image",
        color: "#28a745",
        image: "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQ...",
        brightness: 75,
        position: { x: -20, y: 10 }
    },
    
    textBlocks: [
        {
            id: "block_1704067200000_ghi789",
            text: "❌ 90% людей делают эту *ошибку* в AI",
            position: { x: 50, y: 50 },
            width: 80,
            font: "Bebas Neue",
            size: 18,
            weight: 400,
            color: "#ffffff",
            glow: true,
            isKeyword: true, // Градиент всего блока
            highlightEnabled: true,
            autoHighlight: false, // Отключена авто-подсветка
            keywordColor: "#fd1d1d"
        }
    ],
    
    instagram: {
        enabled: false,
        position: { x: 10, y: 10 }
    }
};
```

### Пример 3: Минимальный текстовый блок

```javascript
const minimalTextBlock = {
    id: "block_1704067200000_min001",
    text: "Нажмите для редактирования",
    
    // Позиционирование (обязательные)
    position: { x: 50, y: 50 },
    width: 60,
    
    // Типографика (со значениями по умолчанию)
    font: "Inter",
    size: 16,
    weight: 700,
    color: "#ffffff",
    
    // Эффекты (по умолчанию отключены)
    glow: false,
    isKeyword: false,
    
    // Подсветка (по умолчанию включена)
    highlightEnabled: true,
    autoHighlight: true,
    keywordColor: "#ff6b6b"
};
```

### Пример 4: Максимально настроенный текстовый блок

```javascript
const advancedTextBlock = {
    id: "block_1704067200000_adv001",
    text: "Изучение *программирования* открывает новые *возможности* карьеры",
    
    // Позиционирование
    position: { x: 25, y: 65 },
    width: 90,
    
    // Типографика
    font: "Playfair Display",
    size: 24,
    weight: 900,
    color: "#2c3e50",
    
    // Эффекты
    glow: true,           // Свечение ключевых слов
    isKeyword: true,      // Градиент всего блока
    
    // Подсветка
    highlightEnabled: true,
    autoHighlight: true,
    keywordColor: "#e74c3c"
};
```

## 🔄 СИНХРОНИЗАЦИЯ ДАННЫХ

### Обратная совместимость с slideStyles

```javascript
// Основные данные в this.slides
this.slides[0].textBlocks[0].position = { x: 60, y: 40 };

// Синхронизация с slideStyles (для обратной совместимости)
this.slideStyles[0].textBlocks[0].position = { x: 60, y: 40 };
```

### Методы работы с данными

```javascript
// Получение текущего слайда
getCurrentSlide() {
    return this.slides[this.currentEditingSlide];
}

// Получение текстовых блоков
getCurrentTextBlocks() {
    const slide = this.getCurrentSlide();
    return slide ? slide.textBlocks : [];
}

// Получение конкретного блока
getTextBlock(blockId, slideIndex = this.currentEditingSlide) {
    const slide = this.slides[slideIndex];
    return slide ? slide.textBlocks.find(block => block.id === blockId) : null;
}

// Обновление свойства блока
updateTextBlockProperty(blockId, property, value) {
    const block = this.getSelectedTextBlock();
    if (block) {
        if (property === 'position') {
            block.position.x = value.x;
            block.position.y = value.y;
        } else {
            block[property] = value;
        }
    }
}
```

## 📋 КЛЮЧЕВЫЕ ОСОБЕННОСТИ

### 1. **Единый источник данных**
- Все данные хранятся в `this.slides`
- `slideStyles` используется только для обратной совместимости

### 2. **Унифицированная система координат**
- Все позиции в процентах (0-100%)
- `position: { x: 50, y: 50 }` = центр слайда

### 3. **Гибкая система подсветки**
- **Ручная**: `*слово*` в тексте
- **Автоматическая**: AI ключевые слова из `autoKeywords`
- **Управляемая**: `highlightEnabled` и `autoHighlight`

### 4. **Эффекты и стили**
- `glow`: свечение применяется только к ключевым словам
- `isKeyword`: градиент применяется ко всему блоку
- `keywordColor`: цвет для подсветки ключевых слов

### 5. **AI интеграция**
- `keywords`: базовые ключевые слова
- `autoKeywords`: ключевые слова от AI для авто-подсветки
- Двухэтапная система: генерация → извлечение ключевых слов

**Эта структура обеспечивает полную гибкость и расширяемость проекта!** 🚀