# 🖱️ DRAG BEHAVIOR FIX - ИСПРАВЛЕНИЕ ПОВЕДЕНИЯ ПЕРЕТАСКИВАНИЯ

## Статус: ✅ ИСПРАВЛЕНО

Улучшено drag-поведение текстовых блоков с правильной логикой включения/выключения и сохранением позиции в state.

## 🎯 Проблемы до исправления

1. **Неточное перетаскивание** - можно было перетаскивать за любую часть блока
2. **Конфликт с редактированием** - drag не отключался во время inline-редактирования
3. **Скачки позиции** - позиция вычислялась из DOM, а не из state
4. **Resize в preview** - ручки изменения размера показывались в режиме просмотра
5. **Resize при редактировании** - ручки не скрывались во время ввода текста

## 🔧 Реализованные исправления

### 1. Улучшенная функция initTextBlockDragging

```javascript
initTextBlockDragging(blockElement, blockId) {
    let isDragging = false;
    let dragEnabled = true;
    
    // Функция для включения/выключения перетаскивания
    const setDragEnabled = (enabled) => {
        dragEnabled = enabled;
        blockElement.style.cursor = enabled ? 'grab' : 'default';
        if (!enabled && isDragging) {
            handleMouseUp(); // Принудительно завершаем drag
        }
    };
    
    // Сохраняем функцию для внешнего доступа
    blockElement._setDragEnabled = setDragEnabled;
}
```

**Ключевые улучшения:**
- ✅ **Контроль drag** - функция `setDragEnabled()` для включения/выключения
- ✅ **Принудительное завершение** - автоматически завершает drag при отключении
- ✅ **Внешний доступ** - функция доступна через `blockElement._setDragEnabled`

### 2. Проверки в handleMouseDown

```javascript
const handleMouseDown = (e) => {
    // Проверяем, что drag включен
    if (!dragEnabled) return;
    
    // Проверяем, что клик именно по тексту, а не по resize handle
    if (e.target.classList.contains('text-block-resize-handle')) return;
    
    // Проверяем, что нет активного inline-редактирования
    if (document.querySelector('.inline-text-editor')) return;
    
    // Проверяем, что клик по самому блоку или его содержимому
    const isTextClick = e.target === blockElement || blockElement.contains(e.target);
    if (!isTextClick) return;
    
    // Получаем текущую позицию из state, а не из DOM
    const block = this.slideStyles[this.currentEditingSlide].textBlocks.find(b => b.id === blockId);
    if (block) {
        startLeft = block.position.x;
        startTop = block.position.y;
    }
};
```

**Ключевые проверки:**
- ✅ **Drag включен** - проверка `dragEnabled`
- ✅ **Клик по тексту** - исключение resize handle
- ✅ **Нет редактирования** - проверка отсутствия `.inline-text-editor`
- ✅ **Позиция из state** - берем координаты из данных, а не из DOM

### 3. Улучшенное inline-редактирование

```javascript
startInlineTextEditing(blockElement, blockId) {
    // Отключаем перетаскивание во время редактирования
    if (blockElement._setDragEnabled) {
        blockElement._setDragEnabled(false);
    }
    
    // Отключаем resize во время редактирования
    const resizeHandle = blockElement.querySelector('.text-block-resize-handle');
    if (resizeHandle) {
        resizeHandle.style.display = 'none';
    }
    
    // ... создание textarea ...
    
    const finishEditing = () => {
        // Включаем обратно перетаскивание
        if (blockElement._setDragEnabled) {
            blockElement._setDragEnabled(true);
        }
        
        // Показываем resize handle
        if (resizeHandle) {
            resizeHandle.style.display = 'block';
        }
    };
}
```

**Ключевые улучшения:**
- ✅ **Отключение drag** - автоматически при начале редактирования
- ✅ **Скрытие resize** - ручки изменения размера скрываются
- ✅ **Восстановление** - все возвращается после завершения редактирования

### 4. Отключение в режиме preview

```javascript
// В updatePreview()
const isInEditor = document.getElementById('editorSection') !== null;

// Resize handle только в редакторе
resizeHandle.style.display = (isInEditor && this.selectedTextBlockId === block.id) ? 'block' : 'none';

// Drag и события только в редакторе
if (isInEditor) {
    this.initTextBlockDragging(blockElement, block.id);
    this.initTextBlockResizing(blockElement, block.id);
} else {
    // В режиме preview отключаем drag
    blockElement.style.cursor = 'default';
}
```

**Ключевые улучшения:**
- ✅ **Определение режима** - проверка наличия `editorSection`
- ✅ **Resize только в редакторе** - ручки скрыты в preview
- ✅ **Drag только в редакторе** - перетаскивание недоступно в preview
- ✅ **Правильный курсор** - `default` в preview, `grab` в редакторе

### 5. Сохранение позиции без скачков

```javascript
const handleMouseMove = (e) => {
    // Вычисляем новую позицию относительно начальной позиции из state
    const newX = Math.max(5, Math.min(95, startLeft + deltaXPercent));
    const newY = Math.max(5, Math.min(95, startTop + deltaYPercent));
    
    // Обновляем позицию в DOM для визуального feedback
    blockElement.style.left = newX + '%';
    blockElement.style.top = newY + '%';
    
    // Обновляем данные в state без скачков
    this.updateTextBlockProperty(blockId, 'position', { x: newX, y: newY });
};
```

**Ключевые улучшения:**
- ✅ **Начальная позиция из state** - `startLeft` и `startTop` из данных
- ✅ **Границы перетаскивания** - ограничение 5%-95% для предотвращения выхода за края
- ✅ **Синхронизация** - обновление и DOM, и state одновременно
- ✅ **Плавное перетаскивание** - без скачков и рывков

## 🧪 Тестирование

### Тестовый файл
`test-drag-behavior.html` включает:

1. **Демо-блок** - интерактивная демонстрация drag-поведения
2. **Контролы** - кнопки для включения/выключения drag и редактирования
3. **Информация о позиции** - отображение текущих координат
4. **Статус** - показ текущего состояния drag, редактирования и позиции
5. **Тест с FlashPost** - проверка интеграции с реальным приложением

### Сценарии тестирования

```javascript
// Тест 1: Включение/выключение drag
enableDrag() / disableDrag()

// Тест 2: Редактирование отключает drag
startEditing() - drag должен отключиться
stopEditing() - drag должен включиться обратно

// Тест 3: Сохранение позиции
resetPosition() - позиция сбрасывается в state и DOM

// Тест 4: Перетаскивание только по тексту
// Клик по resize handle не должен запускать drag
```

## 📋 Результаты исправлений

### До исправления:
- ❌ Drag работал при клике в любом месте блока
- ❌ Drag не отключался во время редактирования
- ❌ Позиция вычислялась из DOM, вызывая скачки
- ❌ Resize handle показывался в preview
- ❌ Resize handle не скрывался при редактировании

### После исправления:
- ✅ **Точное перетаскивание** - только при клике по тексту
- ✅ **Умное отключение** - автоматически отключается при редактировании
- ✅ **Стабильная позиция** - сохраняется в state без скачков
- ✅ **Контекстные элементы** - resize handle только в редакторе
- ✅ **Чистый UI** - никаких лишних элементов в preview

## 🎯 Преимущества

1. **Лучший UX** - интуитивное поведение перетаскивания
2. **Стабильность** - нет конфликтов между drag и редактированием
3. **Точность** - позиция сохраняется точно без искажений
4. **Производительность** - меньше DOM-манипуляций
5. **Чистота кода** - четкое разделение режимов работы

## 🔄 Совместимость

- ✅ **Все существующие функции** работают как прежде
- ✅ **Touch события** поддерживаются на мобильных устройствах
- ✅ **Keyboard shortcuts** не затронуты
- ✅ **Экспорт и шаблоны** работают корректно
- ✅ **Применить ко всем** функционирует правильно

**Статус: DRAG-ПОВЕДЕНИЕ ПОЛНОСТЬЮ ИСПРАВЛЕНО** ✅