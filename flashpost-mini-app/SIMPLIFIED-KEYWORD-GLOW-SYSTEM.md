# 🔧 Упрощенная система keyword/glow

## Задача
Упростить систему keyword/glow:
- **glow только на ключевые слова** (не на весь блок)
- **подсветка только через autoKeywords и ручные** (*слово*)

## Старая система (сложная)
```javascript
// На уровне блока
block.glow = true/false          // свечение всего блока
block.highlightEnabled = true/false  // включение подсветки
block.isKeyword = true/false     // флаг ключевого слова (убрать!)

// На уровне слайда
slide.autoKeywords = []          // автоматические ключевые слова
```

## Новая система (упрощенная)
```javascript
// Убираем из блока
// ❌ block.glow - убрать
// ❌ block.isKeyword - убрать  
// ❌ block.highlightEnabled - убрать

// Оставляем только
slide.autoKeywords = []          // автоматические ключевые слова

// Логика подсветки
// 1. Ручные ключевые слова: *слово* → .manual-keyword + glow
// 2. Автоматические: из autoKeywords → .auto-keyword + glow
// 3. Обычный текст: без подсветки и свечения
```

## Реализация

### 1. Обновить структуру данных
```javascript
// Создание блока (убрать лишние поля)
const newBlock = {
    id: generateUID(),
    text: 'Новый текст',
    x: 50, y: 50, width: 80,
    font: 'Inter', size: 32, weight: 700, color: '#ffffff'
    // ❌ glow: false - убрать
    // ❌ highlightEnabled: true - убрать  
    // ❌ isKeyword: false - убрать
};
```

### 2. Обновить рендеринг
```javascript
// В renderTextBlock - убрать проверку block.glow
// Свечение применяется только к ключевым словам в CSS

// В setTextWithKeywords - всегда обрабатывать ключевые слова
// Не проверять block.highlightEnabled
```

### 3. CSS стили
```css
.manual-keyword {
    background: linear-gradient(45deg, #48cae4, #0077b6);
    color: white;
    padding: 2px 4px;
    border-radius: 4px;
    font-weight: 700;
    filter: drop-shadow(0 0 6px rgba(72, 202, 228, 0.8)); /* glow */
}

.auto-keyword {
    background: linear-gradient(45deg, #ff6b6b, #feca57);
    color: white;
    padding: 2px 4px;
    border-radius: 4px;
    font-weight: 700;
    filter: drop-shadow(0 0 6px rgba(255, 107, 107, 0.8)); /* glow */
}
```

## Преимущества
- ✅ Меньше флагов и сложности
- ✅ Свечение только на важных элементах
- ✅ Четкая логика: ключевые слова = подсветка + glow
- ✅ Проще в использовании и отладке