# 🔧 Чистая DOM система рендеринга

## Задача
Полностью переписать рендеринг на чистый DOM через `createElement`, убрать все HTML-строки и четко разделить режимы edit/preview с drag&drop только в edit.

## Требования
1. ✅ Убрать HTML-строковый рендер
2. ✅ Переписать render слайдов и блоков через createElement
3. ✅ Разделить edit и preview режимы
4. ✅ Drag только в edit режиме

## Архитектура

### Основные методы рендеринга
```javascript
// Главный рендер по режимам
render() {
    if (this.project.mode === 'start') this.renderStart();
    else if (this.project.mode === 'edit') this.renderEditor();
    else if (this.project.mode === 'preview') this.renderPreview();
    else if (this.project.mode === 'export') this.renderExport();
}

// Рендер слайда (универсальный)
renderSlide(slide, mode = 'preview') {
    // mode: 'edit' | 'preview' | 'export'
    const slideElement = document.createElement('div');
    // ... DOM создание
    return slideElement;
}

// Рендер текстового блока
renderTextBlock(block, mode = 'preview') {
    // mode: 'edit' | 'preview' | 'export'
    const blockElement = document.createElement('div');
    // ... DOM создание
    return blockElement;
}
```

### Режимы рендеринга

#### EDIT режим
- Drag & Drop активен
- Текстовые блоки кликабельны
- Показываются ручки изменения размера
- Панель инструментов активна

#### PREVIEW режим  
- Drag & Drop отключен
- Только навигация по слайдам
- Статичные текстовые блоки
- Кнопки действий (редактировать, скачать)

#### EXPORT режим
- Чистые слайды без UI элементов
- Оптимизировано для html2canvas
- Без интерактивности

## Реализация

### 1. Переписать основные методы рендеринга
- `renderPreview()` → DOM
- `renderEditor()` → DOM  
- `renderSlide()` → DOM
- `renderTextBlock()` → DOM

### 2. Создать режимо-специфичные методы
- `createPreviewSlide(slide)`
- `createEditSlide(slide)`
- `createExportSlide(slide)`

### 3. Обновить систему событий
- Drag & Drop только в edit режиме
- Разные обработчики для разных режимов

### 4. Оптимизировать производительность
- Переиспользование элементов
- Минимальные DOM операции
- Эффективное обновление

## Преимущества
- ✅ Лучшая производительность
- ✅ Четкое разделение режимов
- ✅ Более предсказуемое поведение
- ✅ Легче отладка и тестирование
- ✅ Меньше багов с HTML парсингом