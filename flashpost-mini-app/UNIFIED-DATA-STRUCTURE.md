# 🏗️ УНИФИЦИРОВАННАЯ СТРУКТУРА ДАННЫХ

## 📋 Обзор

Полностью переработанная структура данных с единым источником истины в `this.project`. Убраны все дубли, координаты и стили хранятся только в одном месте.

## 🎯 Ключевые принципы

### ✅ Разрешено
- **Единый источник данных**: `this.project.slides`
- **Прямые координаты**: `x: 50, y: 30` в текстовых блоках
- **Встроенные стили**: все стили внутри `textBlocks`
- **Унифицированный доступ**: через методы API

### ❌ Запрещено
- ~~`slideStyles` отдельно~~ 
- ~~Дубли координат~~
- ~~Стили вне `textBlocks`~~
- ~~Множественные источники истины~~

## 🏗️ Структура проекта

```javascript
this.project = {
    // Массив слайдов - единственный источник данных
    slides: [
        {
            id: 'slide_1704067200000_abc123',
            title: 'Hook',
            text: 'Основной текст слайда',
            
            // Фон слайда
            background: {
                type: 'color',        // 'color' | 'image'
                color: '#833ab4',     // Цвет фона
                image: null,          // URL изображения или null
                x: 50,               // Позиция X (0-100%)
                y: 50,               // Позиция Y (0-100%)
                brightness: 100       // Яркость (0-200%)
            },
            
            // Текстовые блоки со всеми стилями
            textBlocks: [
                {
                    id: 'block_1704067200000_def456',
                    text: 'Заголовок слайда',
                    
                    // Позиционирование (БЕЗ вложенности!)
                    x: 50,              // Позиция X (0-100%)
                    y: 30,              // Позиция Y (0-100%)
                    width: 80,          // Ширина (20-100%)
                    
                    // Типографика
                    font: 'Montserrat', // Шрифт
                    size: 48,           // Размер (10-72px)
                    weight: 700,        // Вес (300-900)
                    color: '#ffffff',   // Цвет текста
                    
                    // Эффекты
                    glow: false,        // Свечение ключевых слов
                    isKeyword: false,   // Градиент всего блока
                    
                    // Подсветка
                    keywordColor: '#ff6b6b',     // Цвет ключевых слов
                    highlightEnabled: true,      // Включить *слова*
                    autoHighlight: true          // Включить AI подсветку
                }
            ],
            
            // AI ключевые слова для автоподсветки
            autoKeywords: ['заголовок', 'текст', 'слайд']
        }
    ],
    
    // Состояние приложения
    activeSlideId: 'slide_1704067200000_abc123',  // ID активного слайда
    activeTextBlockId: null,                       // ID активного блока
    mode: 'edit'                                   // Текущий режим
};
```

## 🔧 API для работы с данными

### Слайды

```javascript
// Получение
app.getActiveSlide()                    // Активный слайд
app.getSlideById(slideId)              // Слайд по ID
app.getSlideByIndex(index)             // Слайд по индексу
app.getActiveSlideIndex()              // Индекс активного слайда

// Управление
app.setActiveSlide(slideId)            // Установить активный слайд
app.setActiveSlideByIndex(index)       // Установить по индексу
app.createSlide(data)                  // Создать новый слайд

// Обновление
app.updateSlideProperty(slideId, 'background.color', '#ff0000')
app.updateSlideProperty(slideId, 'background.x', 75)
app.updateSlideProperty(slideId, 'title', 'Новый заголовок')
```

### Текстовые блоки

```javascript
// Получение
app.getActiveTextBlock()               // Активный блок
app.setActiveTextBlock(blockId)        // Установить активный блок

// Создание и удаление
app.createTextBlock(slideId, data)     // Создать блок
app.deleteTextBlock(blockId)           // Удалить блок

// Обновление
app.updateTextBlockProperty(blockId, 'x', 60)
app.updateTextBlockProperty(blockId, 'font', 'Montserrat')
app.updateTextBlockProperty(blockId, 'color', '#ff6b6b')
```

## 📝 Примеры использования

### Создание слайда с текстовым блоком

```javascript
// Создаем слайд
const slide = app.createSlide({
    title: 'Мой слайд',
    text: 'Основной текст',
    background: {
        type: 'color',
        color: '#833ab4',
        x: 50,
        y: 50,
        brightness: 100
    }
});

// Создаем текстовый блок
const block = app.createTextBlock(slide.id, {
    text: 'Заголовок',
    x: 50,        // Центр по X
    y: 30,        // Верхняя треть по Y
    width: 80,    // 80% ширины
    font: 'Montserrat',
    size: 24,
    weight: 700,
    color: '#ffffff'
});

// Устанавливаем как активные
app.setActiveSlide(slide.id);
app.setActiveTextBlock(block.id);
```

### Обновление стилей

```javascript
// Меняем фон слайда
app.updateSlideProperty(slideId, 'background.color', '#fd1d1d');
app.updateSlideProperty(slideId, 'background.x', 25);

// Меняем стили блока
app.updateTextBlockProperty(blockId, 'x', 75);
app.updateTextBlockProperty(blockId, 'font', 'Bebas Neue');
app.updateTextBlockProperty(blockId, 'size', 32);
app.updateTextBlockProperty(blockId, 'glow', true);
```

### Работа с режимами

```javascript
// Переключение режимов
app.setMode('preview');    // Статичный просмотр
app.setMode('edit');       // Полное редактирование
app.setMode('export');     // Интерфейс экспорта

// Проверка режима
if (app.isMode('edit')) {
    // Включаем drag & drop
}

// Получение режима
const currentMode = app.getMode();
```

## 🎨 Рендеринг из единого источника

### Карусель

```javascript
renderCarousel() {
    return this.project.slides.map((slide, index) => {
        const isActive = index === this.getActiveSlideIndex();
        
        return `
            <div class="slide ${isActive ? 'active' : ''}" 
                 data-slide-id="${slide.id}"
                 style="background: ${slide.background.color};">
                
                ${slide.textBlocks.map(block => `
                    <div class="slide-text-block" 
                         data-block-id="${block.id}"
                         style="
                             left: ${block.x}%;
                             top: ${block.y}%;
                             width: ${block.width}%;
                             font-family: ${block.font};
                             font-size: ${block.size}px;
                             font-weight: ${block.weight};
                             color: ${block.color};
                         ">
                        ${this.parseTextWithKeywords(
                            block.text, 
                            block.keywordColor, 
                            block.highlightEnabled, 
                            slide.autoKeywords, 
                            block.glow
                        )}
                    </div>
                `).join('')}
            </div>
        `;
    }).join('');
}
```

## 🔄 Миграция со старой структуры

### Было (дублирование)
```javascript
// ❌ Множественные источники
this.slides = [...];
this.slideStyles = [...];
this.currentSlide = 0;
this.selectedTextBlockId = null;

// ❌ Дубли координат
slideStyles[0].textBlocks[0].position = { x: 50, y: 30 };
slides[0].textBlocks[0].x = 50;
slides[0].textBlocks[0].y = 30;
```

### Стало (единый источник)
```javascript
// ✅ Единый источник истины
this.project = {
    slides: [
        {
            id: 'slide_1',
            textBlocks: [
                {
                    id: 'block_1',
                    x: 50,     // Только здесь!
                    y: 30,     // Только здесь!
                    font: 'Inter',
                    size: 16,
                    // ... все остальные стили
                }
            ]
        }
    ],
    activeSlideId: 'slide_1',
    activeTextBlockId: null
};
```

## ✅ Преимущества новой структуры

### 🎯 Единый источник истины
- Все данные в `this.project.slides`
- Нет дублирования координат
- Нет рассинхронизации данных

### 🚀 Производительность
- Меньше операций синхронизации
- Прямой доступ к данным
- Оптимизированный рендеринг

### 🛠️ Удобство разработки
- Понятная структура данных
- Унифицированный API
- Легкое тестирование и отладка

### 🔧 Расширяемость
- Простое добавление новых свойств
- Гибкая система режимов
- Легкая интеграция с внешними API

## 🧪 Тестирование

### Тестовый файл
`test-unified-structure.html` - полное тестирование новой структуры

**Возможности**:
- 🎮 Создание слайдов и блоков
- 🔧 Обновление свойств
- 🗑️ Удаление элементов
- ✅ Валидация структуры
- 📊 Отладочная панель в реальном времени

### Проверки валидации
- Корректность структуры `project.slides`
- Наличие обязательных полей
- Отсутствие дублирования данных
- Правильность координат и стилей

## 📊 Статистика реализации

- ✅ **Единая структура** `this.project`
- ✅ **15+ методов API** для работы с данными
- ✅ **Полная миграция** рендеринга
- ✅ **Режимная система** интегрирована
- ✅ **Тестовый файл** с валидацией
- ❌ **Дубли данных** полностью устранены

**Унифицированная структура данных готова к использованию!** 🚀